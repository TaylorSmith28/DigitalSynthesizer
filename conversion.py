#input is statemachine file, x is number of input signals, y is number of states
from contextlib import nullcontext

#input is a list of strings representing the state changes
def state_machine_to_truth_table(input, x, y):
    nullcontext

#input is an array representing the truth table
def truth_table_to_karnaugh_map(input):
    nullcontext

#input is an array representing a karnaugh map
def karnaugh_map_to_boolean_logic(input):
    nullcontext

#more conversion maybe needed to go backwards
